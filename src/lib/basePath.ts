// Base path for GitHub Pages deployment
// Change to '' for local development, '/Dev-Folio' for production
const basePath = process.env.NODE_ENV === 'production' ? '/Dev-Folio' : '';
export default basePath;
